name: 'Terraform Plan'

on:
  pull_request:
    branches:
      - main
    paths:
      - 'infrastructure/**'

jobs:
  terraform-plan:
    uses: ./.github/workflows/terraform-actions.yml
    with:
      action: plan
